<% content_for :title {"Admin: Edit Campaign: #{@campaign.title}"} %>
<% breadcrumb :admin_edit_campaign, @campaign %>

<section class="workspace cf">
  <%= render 'form', campaign: @campaign %>
</section>
