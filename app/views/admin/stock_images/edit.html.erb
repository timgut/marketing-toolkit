<% content_for :title, @stock_image.image_file_name %>

<main class="clearfix">
  <section class="workspace cf">
   <%= form_for(@stock_image, url: admin_stock_images_path, html: { method: :post, class: 'edit-form split cf'}) do |f| %>

		<%= render partial: "admin/stock_images/form", locals: { f: f } %>
    
    <div class="buttons cf">
      <%= f.submit "Update Stock Image", class: 'button save' %>
    </div>

          
   <% end %>

  </section><!--workspace-->
      
</main>
