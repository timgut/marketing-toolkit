<%= react_component(
  "PhotoManager", 
  {
    myPhotos:    @my_photos.as_json,
    stockPhotos: @stock_photos.as_json,
    contextCrop: @template.has_blank_image?,
    blankImage:  @template.blank_image_url
  },
  {camelize_props: true}
) %>

<script>ReactRailsUJS.mountComponents()</script>
